<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.zjkh.zgm.provider.user.mapper.UserMapper">

    <resultMap id="user" type="com.zjkh.zgm.facade.model.User">
        <result column="id" property="id" jdbcType="VARCHAR"/>
        <result column="name" property="name" jdbcType="VARCHAR"/>
        <result column="age" property="age" jdbcType="INTEGER"/>
    </resultMap>

    <sql id="user_base">
        id,name,age
    </sql>

    <insert id="add" parameterType="com.zjkh.zgm.facade.model.User">
        INSERT into  USER (id,name,age) VALUES (#{id,jdbcType=VARCHAR},#{name,jdbcType=VARCHAR},#{age,jdbcType=INTEGER})
    </insert>

    <select id="selectUserById" resultMap="user" parameterType="java.lang.Integer">
        SELECT
        <include refid="user_base"/>
        from USER WHERE id=#{id,jdbcType=INTEGER}
    </select>

    <update id="updateUser" parameterType="com.zjkh.zgm.facade.model.User">
        UPDATE USER SET
        <trim prefix="(" suffix=")" suffixOverrides=",">
            <if test="id != NULL">
                id,
            </if>
            <if test="name != NULL">
                name,
            </if>
            <if test="age != NULL">
                age,
            </if>
        </trim>
        <trim prefix="values (" suffix=")" suffixOverrides=",">
            <if test="id !=null">
                #{id,jdbcType=VARCHAR}
            </if>
            <if test="name != null">
                #{name,jdbcType=VARCHAR}
            </if>
            <if test="age != null">
                #{age,jdbcType=INTEGER}
            </if>
        </trim>
    </update>


</mapper>